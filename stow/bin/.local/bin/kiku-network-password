#!/usr/bin/env bash
set -euo pipefail
iface=${1:?interface required}
connection=$(nmcli -t -f GENERAL.CONNECTION dev show "$iface" | cut -d: -f2-)
[[ -n $connection ]] && nmcli -s -g 802-11-wireless-security.psk connection show "$connection" 2>/dev/null || true
